const nomes = ["Renato", "João", "Maria", "José", "Ana"];

for (let i = 0; i < nomes.length; i++) {
    console.log(nomes[i]);
}

const numeros = [1, 2, 3, 4, 5];

for(let j = 0; j < numeros.length; j++) {
    console.log(numeros[j] * 3);
}